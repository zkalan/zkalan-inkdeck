# Zkalan InkDeck 0.5.2 验证记录

日期：2026-09-11。环境：Apple Silicon / macOS 26.5.1 / Swift 6.3.2。

63 项模型检查全部通过。应用回归 193/194 项通过；本次新增的 24 项指针检查全部通过。**不能将完整应用回归记为全部通过**。`smoke-results.json` 保留本次真实结果，`model-results.txt` 为本次模型检查输出。

唯一失败项为 `desktopTouchTargetAtScreenCenter`，与 0.5.1 的既有失败项相同：标记层出现后，系统窗口命中查询仍返回底层窗口，未返回标记层。等待窗口更新后仍未通过，因此尚不能确认这一窗口命中条件在本机环境下可靠；本版没有改动桌面窗口路由。其余捕获状态、暂停点击穿透、键盘路由和光标恢复检查通过。合成输入通过不等于真实 NSTouch 路由已经验证。

## 本次新增的指针验证

主画板与桌面各检查 12 项：进入书写即有位置参考、抬手保留参考并清除实时接触、悬空按空格不落笔、新接触更新位置、抬手后在浅色或透明背景可见、抬手后在纯黑笔迹上可见、使用预览的最终抬起坐标且不落笔、双指导航不产生假接触、视图缩放不改变参考的屏幕相对位置、悬空按 E 不擦除、暂停释放系统鼠标并隐藏画笔参考、恢复书写后不等接触即显示参考。

参考坐标与绘图输入分别保存。显示保留位置不会送入绘图模型，按空格或 E 仍必须有真实或测试接触才能生成笔迹。新增像素检查验证抬手后的虚线圆圈在黑白背景上的可见性。

## 图像制品

- `board-cursor-lifted.png`、`board-cursor-lifted-dark.png`：主画板抬手后在浅色背景和黑色笔迹上保留虚线指针。
- `desktopTool-cursor-lifted.png`、`desktopTool-cursor-lifted-dark.png`：透明桌面层中的同等状态；透明 PNG 在不同查看器中可能显示为黑色底。
- `board-cursor-dark.png`、`desktopTool-cursor-dark.png`：接触时黑色笔迹上的实线指针。
- 其余图片为本轮回归生成的画板、工具栏、帮助、调色、擦除和合成桌面演示。

已查看抬手状态的主画板黑底及透明桌面图片。所有制品均由应用自身渲染，不包含用户桌面截图或个人草稿。

## 验证边界与安装

测试使用真实 AppKit 窗口与合成触控/压力，不能替代实际手感、多台物理显示器和会议共享效果。抬手时显示的是最后位置，不能追踪悬空手指。应用采用临时签名，未进行 Apple 公证。安装脚本正常退出旧版并备份应用和草稿；打包时验证签名，公开包附 SHA-256。桌面讲解请共享整个屏幕。
