# v0.3 验证记录

日期：2026-09-10。环境：Apple Silicon / macOS 26.5.1 / Swift 6.3.2。

- arm64 原生应用编译成功，安装包本地代码签名校验通过，版本 0.3.0。沿用 `local.trackpad-ink.app.v2` 和 `~/Applications/触控板手写 2.app`。
- 49 项模型检查通过。新增覆盖 2%/10% 低压力响应、曲线单调性、灵敏度变化、零压力速度辅助、缩放不影响速度响应、普通笔纯压感模式、60/120 Hz 平滑一致性和独立压力事件不重置移动速度。
- 40 项应用检查全部通过，结果见 `smoke-results.json`。新增覆盖单键 Z/Y、原生事件派发、菜单键盘入口、Cmd-Z/Shift-Cmd-Z 兼容、Cmd-H 放行、文本编辑/对话框保护、Enter 开始/结束、空格预览、单键缩放/原位、菜单速查、H 打开/收起帮助。
- 默认窗口内容区 1180 × 830 点，白色绘画区域 1166 × 728.75 点，占 86.76%。保持触控板宽高比；其他窗口尺寸或触控板比例下的占比会不同。
- 已查看 `window-smoke.png`、`window-detail.png`、`window-minimum.png`、`help-smoke.png`、`light-touch-smoke.png`。两行工具栏与状态栏完整，帮助文字正常换行。检查期间发现帮助窗口被文字固有宽度撑大，已增加明确宽度与换行约束，并改为即时开关；最终检查通过。
- 低压力图片从上到下为 0%、2%、10% 合成原始压力，笔迹有速度辅助变化，较小压力使整体笔宽增加。硬件压力条始终是硬件读数，测试不会将合成样本计作硬件输入。
- 导出包含屏幕外笔迹，普通笔与毛笔中心线填充连续、光标隐藏/恢复配对、双指导航不留墨等原有检查通过。
- 安装时先完成暂存与签名验证，确认旧应用退出后，备份旧程序和 `draft-v2.json` 再更新。备份位置及安装校验见 `install-results.json`；草稿格式沿用 v2。

模型与应用检查使用合成接触/压力，不读取或修改用户草稿。截图为应用自身的离屏渲染，不是实物手指试写。实际 Force Touch 事件、端到端延迟、书写手感及长时间大作品性能仍需用户在实体触控板上试用。轻触增强的速度辅助与硬件压力有明确区分，公开 `.primaryGeneric` 事件流的点击门槛无法通过软件曲线消除。
