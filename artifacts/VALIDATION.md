# 验证记录

日期：2026-09-10。环境：Apple Silicon / macOS 26.5.1 / Swift 6.3.2。

- 原生 arm64 应用构建成功，Info.plist 校验和本地代码签名校验通过。
- 19 项输入模型检查通过：绝对落点、抬手断笔、接触身份变化、结束采样、落点预览、多指暂停、取消输入、撤销重做、草稿往返序列化及边界约束。
- 桌面会话中的应用冒烟测试通过，详细结果见 `smoke-results.json`：书写模式进入/退出、系统光标隐藏/解除相对移动/恢复调用、原光标位置恢复、独立两笔绘制及 PNG 导出。
- 已人工查看 `window-smoke.png` 和 `export-smoke.png`：中文界面可读，两条笔画分处上下不同位置，无连接线；PNG 方向正确，不含辅助方格和笔尖。
- 已复制到 `~/Applications/触控板手写.app`，通过 Launch Services 启动。
- 外部界面自动化因 Computer Use 的辅助功能和屏幕录制权限未授予而未完成。没有修改系统权限。
- 真实手指触控事件、硬件延迟及连续中文书写手感仍需用户在触控板上验证。自动测试使用合成模型坐标，不能证明真实触控输入已通过。

首次从受限 shell 直接启动时，macOS 的桌面应用注册服务无法连接，进程在 `NSApplication.shared` 初始化处退出。在获准的桌面会话中运行同一构建后测试通过。
